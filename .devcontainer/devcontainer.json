{
  "image": "ghcr.io/calvinw/ai-course-devcontainer:latest",
  "postCreateCommand": "bash .devcontainer/setup-codex.sh && (setup-env.sh && curl -LsSf https://astral.sh/uv/install.sh | sh && install-mcps.sh && setup-skills.sh && skillshare install github.com/anthropics/skills/skill-creator && sync-skills.sh || true)",
  "secrets": {
    "STITCH_API_KEY": "STITCH_API_KEY"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ritwickdey.LiveServer"
      ]
    }
  }
}
