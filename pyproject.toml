[project]
name = "lca-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "fastapi",
    "uvicorn[standard]",
    "requests",
    "olca-ipc",
    "olca-schema",
    "pyyaml",
    "numpy",
]
